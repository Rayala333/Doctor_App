interface CounterState {
    fistName: string;
    lastName: string;
    phoneNumber: string;
    webSite: string;
    address: string;
    specialization: string;
    experience: number;
    timings: string[]; // Assuming timings are strings
  }
  
  export default CounterState;